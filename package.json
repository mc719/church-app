{
  "name": "church-data",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "build": "npm install && npm --prefix client install --include=dev && npm --prefix client run build",
    "security:secrets": "node scripts/secret-scan.js",
    "security:test": "node scripts/security-smoke.js",
    "security:audit": "npm audit --omit=dev --audit-level=high && npm --prefix client audit --omit=dev --audit-level=high",
    "migrate": "node scripts/migrate.js",
    "backup:verify": "node scripts/verify-backup-restore.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.17.2",
    "zod": "^3.24.1"
  },
  "type": "commonjs"
}
